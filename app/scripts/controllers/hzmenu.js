'use strict';

/**
 * @ngdoc function
 * @name webAdminApp.controller:HzmenuCtrl
 * @description
 * # HzmenuCtrl
 * Controller of the webAdminApp
 */
angular.module('webAdminApp')
  .controller('HzmenuCtrl', function ($scope) {
     $scope.page = {
      title: 'Horizontal menu Layout',
      subtitle: 'Place subtitle here...'
    };
  });
